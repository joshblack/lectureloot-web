<?php

class ClassActionsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('classactions')->truncate();

		$classactions = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('classactions')->insert($classactions);
	}

}
